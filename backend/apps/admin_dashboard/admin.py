"""
Django admin configuration for admin_dashboard app.

This app doesn't have models, so no admin registration needed.
Admin operations are handled through the API endpoints.
"""

from django.contrib import admin

# No models to register - admin operations handled via API
