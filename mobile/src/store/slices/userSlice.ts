import { createSlice } from '@reduxjs/toolkit';

// Placeholder for user slice
const userSlice = createSlice({
  name: 'user',
  initialState: {},
  reducers: {},
});

export default userSlice.reducer;
