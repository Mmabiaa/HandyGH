import { createSlice } from '@reduxjs/toolkit';

// Placeholder for provider slice
const providerSlice = createSlice({
  name: 'provider',
  initialState: {},
  reducers: {},
});

export default providerSlice.reducer;
