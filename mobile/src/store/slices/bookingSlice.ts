import { createSlice } from '@reduxjs/toolkit';

// Placeholder for booking slice
const bookingSlice = createSlice({
  name: 'booking',
  initialState: {},
  reducers: {},
});

export default bookingSlice.reducer;
